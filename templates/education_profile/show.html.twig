{% extends 'base.html.twig' %}

{% block title %}EducationProfile{% endblock %}

{% block body %}
    <h1>EducationProfile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ education_profile.id }}</td>
            </tr>
            <tr>
                <th>Education_profile_name</th>
                <td>{{ education_profile.educationProfileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_education_profile_index') }}">back to list</a>

    <a href="{{ path('app_education_profile_edit', {'id': education_profile.id}) }}">edit</a>

    {{ include('education_profile/_delete_form.html.twig') }}
{% endblock %}
