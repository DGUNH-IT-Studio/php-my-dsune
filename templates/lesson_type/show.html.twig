{% extends 'base.html.twig' %}

{% block title %}LessonType{% endblock %}

{% block body %}
    <h1>LessonType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson_type.id }}</td>
            </tr>
            <tr>
                <th>Lesson_type_name</th>
                <td>{{ lesson_type.lessonTypeName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lesson_type_index') }}">back to list</a>

    <a href="{{ path('app_lesson_type_edit', {'id': lesson_type.id}) }}">edit</a>

    {{ include('lesson_type/_delete_form.html.twig') }}
{% endblock %}
