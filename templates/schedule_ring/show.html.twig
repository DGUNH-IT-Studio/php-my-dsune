{% extends 'base.html.twig' %}

{% block title %}ScheduleRing{% endblock %}

{% block body %}
    <h1>ScheduleRing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ schedule_ring.id }}</td>
            </tr>
            <tr>
                <th>Lesson_num</th>
                <td>{{ schedule_ring.lessonNum }}</td>
            </tr>
            <tr>
                <th>Time_start</th>
                <td>{{ schedule_ring.timeStart ? schedule_ring.timeStart|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Time_end</th>
                <td>{{ schedule_ring.timeEnd ? schedule_ring.timeEnd|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_schedule_ring_index') }}">back to list</a>

    <a href="{{ path('app_schedule_ring_edit', {'id': schedule_ring.id}) }}">edit</a>

    {{ include('schedule_ring/_delete_form.html.twig') }}
{% endblock %}
